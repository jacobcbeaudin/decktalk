---
title: Troubleshooting
description: Take the code DeckTalk printed from the symptom you saw to the change that clears it.
---

DeckTalk answers in codes. There are nine error codes and fifty-one finding codes, every one of them
carries its own page, and this page is the map from what you saw to which code it was.

Each code's page carries its sentence, its certainty and the settings keys that move it. They are
generated from the same enums the tool prints from, so they are the authority on what a code means.
This page is the other half: what the symptom looks like, which command to run to make the code
appear as data, and what to change.

## The two kinds of answer

An **error** means DeckTalk could not run, so nothing was judged. It prints one block and the command
stops.

```text
error[INPUT]: cue found 1 thing(s) that are certainly wrong, so the build stopped there rather than carrying them into the film.
  hint: Fix these and run the build again:
  CUE_UNRESOLVED at 1.1:title: the cue 1.1:title waits for 'a phrase nobody says' and section 1 speaks 38 words, none of which is that phrase.
  docs: https://docs.decktalk.ai/reference/errors/INPUT
```

A **finding** is a judgement DeckTalk made about your files or your film. The command ran, and this
is what it measured. It prints one line per judgement, then one count line.

```text
cues.json: CUE_MISSING the deck declares 12 moment(s) and there is no cues.json, so nothing gives any of them a second.
  fix (safe): Write cues.json with a row for each of the 12 moment(s) the deck declares, each waiting for the phrase you write in its `on`.
Found 1 finding, 1 certain. 1 fixable with --fix.
```

The line opens with the file and the line number when the finding knows them, and with the object it
judges when it does not. The `fix` line under it appears only when the finding carries one.

Under `--json`, `error` is filled for the first and null for the second, and `findings` carries the
second as an array of objects.

| Exit | What it means |
|---|---|
| 0 | The command ran and found nothing at or above the threshold. |
| 1 | The command ran and found something at or above the threshold. |
| 2 | The command line was refused, which is `USAGE` or `APPROVAL`. |
| 3 | The command could not run, which is every other error code. |
| 130 | The caller stopped the run, which is `CANCELLED`. |

[Exit codes](/reference/cli#exit-codes), [Error codes](/reference/cli#error-codes) and
[Finding codes](/reference/cli#finding-codes) list all of them in one place.

## The three commands to run first

| Command | What it answers |
|---|---|
| `decktalk doctor` | Whether this machine has Chromium, ffmpeg, ffprobe and KaTeX, and whether a voice key is set. |
| `decktalk status` | What is written, what is built and what is stale in this project. |
| `decktalk check` | Whether `script.md`, `cues.json` and the pages hold together, and what a voiced run would cost. |

None of the three produces a film and none of them spends. `decktalk check --no-pages` goes further
and opens no browser at all, which makes it the one to run on a machine with no Chromium and inside a
commit hook. It names every judgement it could not reach rather than passing quietly.

```text
no page was opened, so these judgements were not reached: CUE_MISSING, CUE_UNKNOWN, CUE_OVERLAP,
PAGE_MOTION_OVERRUN, PAGE_STAGGER_OVERRUN, PAGE_NO_DESCRIPTION, PAGE_SWAP_APART, PAGE_CDN_ASSET,
CUE_NO_CHANGE, CUE_THIN_CHANGE, PAGE_THIN_DRAW, CUT_POP.
```

`decktalk check --no-frames` keeps the browser and drops the freeze comparison, which is the faster
half when you only want the reading of the files and the page.

## Apply the fix a finding carries

A finding may carry a `fix`. It is one of three kinds, and it declares how far it may be trusted.

| Kind | What it is |
|---|---|
| `edit` | One or more changes to files, each naming exactly one of a JSON pointer, a TOML key or a line. |
| `setting` | A settings key and the value that clears the judgement. |
| `command` | The whole command line to run, one argument per element. |

| Applicability | What happens |
|---|---|
| `safe` | `--fix` applies it. It cannot lose your work. |
| `unsafe` | It is printed and never applied by `--fix`, because applying it could lose your work. |
| `display` | Only a person can make this change, so it is printed for you to make by hand. |

`decktalk check --fix`, `decktalk doctor --fix` and `decktalk build --fix` apply every safe fix and
print every other one. `--no-fix` applies none. In the library it is
`Project.apply(finding, unsafe=False)`, and passing `unsafe=True` is the only way an unsafe fix is
ever written.

The count line tells you before you run it: `Found 1 finding, 1 certain. 1 fixable with --fix.`

A safe fix does the boring part and leaves the judgement to you. `check --fix` on a project with no
cue file writes every row the deck declares with an empty `on`, so the next run refuses with an
`INPUT` error naming the first empty phrase. That is the fix working. Fill in the phrases and run it
again.

## Carry on past a code, or change what fails a run

| Flag | What it does |
|---|---|
| `--allow CODE` | Carry on past this code. It repeats, so `--allow CUE_OVERLAP --allow PAGE_SLIDE_UNUSED` forgives both. |
| `--fail-on certain` | The default. A certain finding fails the run and an uncertain one does not. |
| `--fail-on any` | Any finding fails the run. |
| `--fail-on never` | No finding fails the run, so the exit code reports only whether the command could run. |

Both work on every command that judges, which is `doctor`, `check`, `storyboard`, `narrate`, `cue`,
`record`, `soundscape`, `assemble`, `verify` and `build`.

`build` is stricter than its parts, and this catches people out. When a stage inside a build judges
something certain about its own work, the build stops there and reports an `INPUT` error whose hint
lists those codes, because carrying on would deliver a film that is wrong in a way nobody has to
watch it to discover. `--fail-on never` does not lift that, and `--allow` lifts it for exactly one
code, `CUE_UNKNOWN`, which is the one certain judgement an author may knowingly keep while a deck is
under construction.

So when a build stops on a code, run that stage alone to get the findings back as data:

```bash
decktalk cue --json | jq '.findings'
```

## From a symptom to a code

| What you saw | The code | What to do about it |
|---|---|---|
| A command said ffmpeg or Chromium is missing or failed | [TOOL](/reference/errors/TOOL) | [A missing toolchain](#a-missing-toolchain) |
| `doctor` listed a component with no version and no path | [FILE_MISSING](/reference/findings/FILE_MISSING) | [A missing toolchain](#a-missing-toolchain) |
| A file the project names is not on disk | [FILE_MISSING](/reference/findings/FILE_MISSING) | [A missing toolchain](#a-missing-toolchain) |
| The command exited 2 without doing anything | [USAGE](/reference/errors/USAGE) | [A refused command line](#a-refused-command-line) |
| A run refused because it would have spent money | [APPROVAL](/reference/errors/APPROVAL) | [A refused command line](#a-refused-command-line) |
| A command said another writer holds the build directory | [LOCKED](/reference/errors/LOCKED) | [A locked project](#a-locked-project) |
| A reveal never happened, and the message names a cue | [CUE_UNRESOLVED](/reference/findings/CUE_UNRESOLVED), [CUE_MISSING](/reference/findings/CUE_MISSING), [CUE_UNKNOWN](/reference/findings/CUE_UNKNOWN), [PAGE_CUE_UNKNOWN](/reference/findings/PAGE_CUE_UNKNOWN) | [An unresolved cue](#an-unresolved-cue) |
| Two reveals ran into each other | [CUE_OVERLAP](/reference/findings/CUE_OVERLAP) | [An unresolved cue](#an-unresolved-cue) |
| A cue resolved and nothing on screen changed | [CUE_NO_CHANGE](/reference/findings/CUE_NO_CHANGE), [CUE_THIN_CHANGE](/reference/findings/CUE_THIN_CHANGE), [PAGE_THIN_DRAW](/reference/findings/PAGE_THIN_DRAW) | [A picture that does not change](#a-picture-that-does-not-change) |
| The film is black somewhere, stops moving, or a section is short | [PAGE_BLACK](/reference/findings/PAGE_BLACK), [PAGE_STALLED](/reference/findings/PAGE_STALLED), [PAGE_TRUNCATED](/reference/findings/PAGE_TRUNCATED) | [A recording that opens black or stalls](#a-recording-that-opens-black-or-stalls) |
| Something on the page threw, or a wait never settled | [PAGE_RENDER_THREW](/reference/findings/PAGE_RENDER_THREW), [PAGE_ENTER_THREW](/reference/findings/PAGE_ENTER_THREW), [PAGE_HANDLER_THREW](/reference/findings/PAGE_HANDLER_THREW), [PAGE_SLIDE_HANDLER_THREW](/reference/findings/PAGE_SLIDE_HANDLER_THREW), [PAGE_WAIT_REJECTED](/reference/findings/PAGE_WAIT_REJECTED), [PAGE_WAIT_UNSETTLED](/reference/findings/PAGE_WAIT_UNSETTLED) | [A recording that opens black or stalls](#a-recording-that-opens-black-or-stalls) |
| A reveal arrived after its word | [CUE_OFF](/reference/findings/CUE_OFF), [PAGE_WORD_LATE](/reference/findings/PAGE_WORD_LATE), [CUE_NO_ONSET](/reference/findings/CUE_NO_ONSET) | [A late landing](#a-late-landing) |
| A motion was still playing when the next cue was read | [PAGE_MOTION_OVERRUN](/reference/findings/PAGE_MOTION_OVERRUN), [PAGE_STAGGER_OVERRUN](/reference/findings/PAGE_STAGGER_OVERRUN), [PAGE_APPEAR_TOO_LONG](/reference/findings/PAGE_APPEAR_TOO_LONG) | [A late landing](#a-late-landing) |
| A cut slices a word, or the picture pops at a seam | [CUT_SPEECH](/reference/findings/CUT_SPEECH), [CUT_POP](/reference/findings/CUT_POP) | [A late landing](#a-late-landing) |
| The film is too quiet, too loud or too peaky | [MIX_LOUDNESS](/reference/findings/MIX_LOUDNESS) | [A loudness miss](#a-loudness-miss) |
| The page fetched something from another server | [PAGE_CDN_ASSET](/reference/findings/PAGE_CDN_ASSET) | [A page that loads an asset from a network origin](#a-page-that-loads-an-asset-from-a-network-origin) |
| An attribute or a value the contract does not declare | [PAGE_UNKNOWN_ATTR](/reference/findings/PAGE_UNKNOWN_ATTR), [PAGE_BAD_VALUE](/reference/findings/PAGE_BAD_VALUE) | [A page the contract refuses](#a-page-the-contract-refuses) |
| A slide, a scene or a template is wrong in the markup | [PAGE_SLIDE_NO_ID](/reference/findings/PAGE_SLIDE_NO_ID), [PAGE_SLIDE_DOUBLED](/reference/findings/PAGE_SLIDE_DOUBLED), [PAGE_SLIDE_UNUSED](/reference/findings/PAGE_SLIDE_UNUSED), [PAGE_TEMPLATE_IGNORED](/reference/findings/PAGE_TEMPLATE_IGNORED), [PAGE_SCENE_EMPTY](/reference/findings/PAGE_SCENE_EMPTY) | [A page the contract refuses](#a-page-the-contract-refuses) |
| A moment has no owner, no element or the wrong order | [PAGE_MOMENT_UNKNOWN](/reference/findings/PAGE_MOMENT_UNKNOWN), [PAGE_MOMENT_ORDER](/reference/findings/PAGE_MOMENT_ORDER), [PAGE_NO_OWNER](/reference/findings/PAGE_NO_OWNER), [PAGE_FREEZE_CUE_UNKNOWN](/reference/findings/PAGE_FREEZE_CUE_UNKNOWN) | [A page the contract refuses](#a-page-the-contract-refuses) |
| Two elements claim one thing | [PAGE_SWAP_AMBIGUOUS](/reference/findings/PAGE_SWAP_AMBIGUOUS), [PAGE_PREVIEW_AMBIGUOUS](/reference/findings/PAGE_PREVIEW_AMBIGUOUS), [PAGE_SWAP_APART](/reference/findings/PAGE_SWAP_APART) | [A page the contract refuses](#a-page-the-contract-refuses) |
| A change reaches the picture and not the transcript | [PAGE_NO_DESCRIPTION](/reference/findings/PAGE_NO_DESCRIPTION), [PAGE_CLASS_UNDESCRIBED](/reference/findings/PAGE_CLASS_UNDESCRIBED), [PAGE_CLASS_NOT_REDUCED](/reference/findings/PAGE_CLASS_NOT_REDUCED) | [A page the contract refuses](#a-page-the-contract-refuses) |
| An equation shows its source, or an error where the maths should be | [PAGE_KATEX_MISSING](/reference/findings/PAGE_KATEX_MISSING), [PAGE_KATEX_ERROR](/reference/findings/PAGE_KATEX_ERROR) | [A page the contract refuses](#a-page-the-contract-refuses) |
| A word-synced line, a count or a stagger has nothing to work on | [PAGE_WORDS_NOT_FOUND](/reference/findings/PAGE_WORDS_NOT_FOUND), [PAGE_STAGGER_EMPTY](/reference/findings/PAGE_STAGGER_EMPTY) | [A page the contract refuses](#a-page-the-contract-refuses) |
| The script is not ready for a voice to read it | [TAKE_PLACEHOLDER](/reference/findings/TAKE_PLACEHOLDER), [TAKE_SPOKEN_SYMBOL](/reference/findings/TAKE_SPOKEN_SYMBOL) | [A script that is not ready to be voiced](#a-script-that-is-not-ready-to-be-voiced) |

Every code links to its own page, which carries that code's sentence, its certainty and the settings
keys that move it. The rest of this page says what to change.

## A missing toolchain

DeckTalk needs Chromium to record a page and ffmpeg with ffprobe to measure, cut and encode. It
fetches both the first time a command needs them and keeps them in a per-user cache.

**What you see.** `error[TOOL]` when a command needed one of them and could not run it, or a
`FILE_MISSING` finding from `doctor` for each component this machine has not got.

**What to do.**

1. Run `decktalk doctor`. It prints a row per component with its version and where it came from, and
   it never fetches anything.
2. Run `decktalk install`, or `decktalk doctor --fix`, which applies the safe `command` fix that
   each `FILE_MISSING` carries. Both fetch the toolchain up front rather than in the middle of a
   build.
3. On Linux, `decktalk install` also asks Playwright for Chromium's system libraries, which it
   installs through sudo, so it may ask for a password. A build never does that, because a build
   runs unattended and a password prompt is a hang rather than a question. That is why a Chromium
   that will not launch says to run `decktalk install`.

**The knobs.** `[tools] ffmpeg` and `[tools] ffprobe` name binaries the machine already has.
`[tools] cache_dir` moves what DeckTalk fetches. `[record] browser_path` records with a browser the
machine already has and skips the Chromium download entirely. Set
`PLAYWRIGHT_DOWNLOAD_HOST` to a mirror when a proxy blocks Playwright's own host, and
`PLAYWRIGHT_BROWSERS_PATH` when the home directory is read-only or the browser is shared.

`FILE_MISSING` is also what a stage raises when a file the project names is not on disk, such as an
effect a `[[mix.effects]]` row cues or a clip a section names. There the fix is to write the file or
correct the path, and `decktalk soundscape` is what generates the sound files the project describes.

## A refused command line

Exit 2 means DeckTalk refused before doing anything, and a retry of the same line is refused again.

**`USAGE`.** The parser refused the line. The message names what is wrong with it. Run
`decktalk <command> --help`, or `decktalk schema` for the whole instruction set as one JSON object.

**`APPROVAL`.** A run that would spend money was not approved. `narrate` and `soundscape` are the two
stages that spend.

| What you want | What to pass |
|---|---|
| No spending at all | `--no-voice`, which writes placeholder narration that still carries a real word clock |
| Spend without being asked | `--spend` |
| Spend up to a ceiling | `--max-cost N`, which refuses before the first call when the most the run could cost is over N US dollars |
| Know the price first | `decktalk check`, which prices the run and buys nothing |

`--no-input` never prompts. It takes the safe default, or it refuses and names the flag that would
have answered, which is what makes a non-interactive run deterministic.

In the library there is no terminal to ask, so nothing spends without `voice=decktalk.Voicing.PAID`,
and `ApprovalRequired` is raised rather than money being spent.

## A locked project

**What you see.** `error[LOCKED]`, naming the process and the run that holds the directory, with the
hint to wait for that run or stop it.

One writing run at a time holds `build/.lock`, which carries the process id and the run id of the
writer. A second writing run is refused rather than allowed to overwrite the first one's work.

**What to do.**

1. Find the run named in the message. It is usually a `--watch` build in another terminal.
2. Let it finish, or stop it. The lock file is removed when the run ends, including when it is
   interrupted.
3. A lock left behind by a process that is gone is not an obstacle. The next run notices, prints a
   warning saying it took the lock, and carries on.
4. To read the project while a build is running, use `decktalk status` or
   `decktalk check --no-pages`. Neither writes, so neither takes the lock.

## An unresolved cue

A cue is one thing written in two files. The page declares a moment with a local name, and the
runtime qualifies it into a wire id, which is the slide id, a colon and the local name. So
`data-in="expand"` inside `<template data-slide="4.1">` is the wire id `4.1:expand`, and that is
what `cues.json` carries in its `cue` field. Four codes say which half is missing.

| Code | Which half is missing | Where the change goes |
|---|---|---|
| [CUE_MISSING](/reference/findings/CUE_MISSING) | The page's half exists and the file's does not. | Add the row to `cues.json`, or let `--fix` add it. |
| [CUE_UNKNOWN](/reference/findings/CUE_UNKNOWN) | The file's half exists and the page's does not. | Draw the element, correct the id, or delete the row. |
| [CUE_UNRESOLVED](/reference/findings/CUE_UNRESOLVED) | Both halves exist and the narration has no such phrase. | Change the `on` phrase, or change the script. |
| [PAGE_CUE_UNKNOWN](/reference/findings/PAGE_CUE_UNKNOWN) | The deck declares no such cue at all. | Name the cue in a moment attribute on the slide, or take it out. |

**Reading `CUE_UNRESOLVED`.** The message names the phrase and how many words the section actually
speaks.

```text
1.1:title: CUE_UNRESOLVED the cue 1.1:title waits for 'a phrase nobody says' and section 1 speaks 38 words, none of which is that phrase.
```

Run `decktalk words --section 1` to see every spoken word with its start and its end, which is the
clock a phrase is written against. Match the phrase to words that are really there. A phrase is
matched without case by default, and the row's own keys narrow it: `occurrence` picks which of
several matches to take, `case_sensitive` makes the match exact, `offset` moves the second the cue
fires, and `verify` set to false leaves the cue out of the measurement.

**Reading `CUE_MISSING`.** This one carries a safe `edit` fix that writes a row per declared moment
with an empty `on` for you to fill. On a `cues.json` laid out in a way DeckTalk cannot place a row
into, the fix is `display` instead and prints the rows for you to paste.

**Reading `CUE_UNKNOWN`.** When DeckTalk can see which cue the row was probably meant to be, it
offers an `unsafe` rename fix, which is unsafe because renaming an id the author typed is a reading
rather than a fact. `--allow CUE_UNKNOWN` is the one way to carry a certain code through a build,
because a deck under construction lists the cues of slides nobody has drawn yet.

**Which command sees what.** The `cue` stage compares `cues.json` against the catalog the page
published into its recordings, so a section whose page has never been recorded is left unjudged
rather than guessed at. `decktalk check` makes the same comparison against a page it opens live,
which is why a fresh project shows `CUE_MISSING` and `CUE_UNKNOWN` from `check` and not from `cue`.

`CUE_OVERLAP` is the neighbouring uncertain code. It fires when two cues resolve close enough
together that one motion is still playing when the next begins, and the message says how far apart
they landed and how long the first was still running.

## A picture that does not change

A cue that resolves to a second means nothing if nothing on screen moves at that second. Three codes
measure that, all of them by comparing frames.

| Code | Certainty | What it usually means |
|---|---|---|
| [CUE_NO_CHANGE](/reference/findings/CUE_NO_CHANGE) | certain | The element never arrived, or it was already on screen before its cue. |
| [CUE_THIN_CHANGE](/reference/findings/CUE_THIN_CHANGE) | uncertain | The element arrived and it is too small for a viewer to notice. |
| [PAGE_THIN_DRAW](/reference/findings/PAGE_THIN_DRAW) | uncertain | A stroke that draws itself covers too little ground to be read as a change. |

**What you see.** The message carries the measured share of the frame and the floor it is under.

```text
build/frames/01/slide-1.1-after-1.1_title.png: CUE_NO_CHANGE freezing the slide either side of 1.1:title changes 0.00 percent of the frame, which is under the 0.10 percent floor, so the reveal would not be measured at all.
```

**What to do.**

1. Look at the two frames the message names. `decktalk check` writes a frame either side of every
   cue under `build/frames/`, and draws them all onto `build/storyboard.html`.
2. `decktalk storyboard --before 1.1:title --after 1.1:title` freezes just that pair, which is the
   fastest way to see what the reveal actually did.
3. Make the reveal bigger. A reveal that changes a few pixels is a reveal a viewer misses too, so
   the usual fix is on the page rather than in the settings.
4. Check the element really has a moment attribute and that its cue is the one that fired. An
   element that only changes class needs `data-class`, and a class change that animates needs the
   page's own stylesheet to do the animating.

**The knobs.** `verify.changed_share_min_percent` is the floor, as a percentage of the frame.
`verify.thin_change_factor` is how many times that floor a change must cross to stop being thin.
`verify.probe_diff_luma` is the per-pixel luma difference that counts a pixel as changed, and
`verify.probe_delays_seconds` is when after the cue the frames are compared. `video.crf` moves all
three, because a harder compression smooths away small changes before anything measures them. Read
any of them whole with `decktalk config explain verify.changed_share_min_percent`.

## A recording that opens black or stalls

These three are measured against a recording rather than against a file you wrote. `record` measures
all three on the file it has just written, and `verify` measures `PAGE_BLACK` again on the finished
film.

| Code | Look at | The knob that decides it |
|---|---|---|
| [PAGE_BLACK](/reference/findings/PAGE_BLACK) | Whether the page had drawn anything by that second. | `verify.black_max_luma`, `verify.after_dip_seconds` |
| [PAGE_STALLED](/reference/findings/PAGE_STALLED) | Whether something on the page threw and stopped the drawing. | `record.frame_gap_max_ms` |
| [PAGE_TRUNCATED](/reference/findings/PAGE_TRUNCATED) | Whether the section's page was still working when its narration ended. | `record.truncated_slack_seconds` |

**Where the black usually comes from.** The recorder covers the page in magenta from its first paint
until the narration clock starts, and takes the first clean frame after that run of cover frames as
t=0. When the scan finds no cover frames, the run falls back to the first painted frame plus the
settle, and failing that to `record.fallback_first_paint_seconds` plus the settle. It says so when it
does:

```text
Section 2 shows no cover, so narration t=0 is a guess at 1.6s (the first painted frame and the settle), and every reveal in the section moves with it.
```

Treat that warning as the first thing to fix, because a guessed start moves every reveal in the
section together and every later code in that section is measuring against the wrong zero.

**Where a stall usually comes from.** Something on the page threw, so nothing drew after it. The page
reports that itself, through the probe, as one of these:

| Code | Which of your own code to open |
|---|---|
| [PAGE_RENDER_THREW](/reference/findings/PAGE_RENDER_THREW) | The slide's render. |
| [PAGE_ENTER_THREW](/reference/findings/PAGE_ENTER_THREW) | The slide's enter. |
| [PAGE_SLIDE_HANDLER_THREW](/reference/findings/PAGE_SLIDE_HANDLER_THREW) | That slide's own handler for the cue named in the message. |
| [PAGE_HANDLER_THREW](/reference/findings/PAGE_HANDLER_THREW) | The handler registered for the cue named in the message. |
| [PAGE_WAIT_REJECTED](/reference/findings/PAGE_WAIT_REJECTED) | Whatever the page asked the runtime to wait for, which rejected. |
| [PAGE_WAIT_UNSETTLED](/reference/findings/PAGE_WAIT_UNSETTLED) | The same wait, which never settled at all. |

**What to do.**

1. Run `decktalk serve` and open the page in your own browser. It serves the deck directory and the
   files `decktalk.toml` declares, and nothing else, so what you see is what the recorder sees.
2. Read the browser console. These six codes are the page's own report from the browser, so the
   detail behind each one is there.
3. Run `decktalk check`, which opens the same page through the same probe without recording
   anything.
4. For a truncation, look at whether the section's page is still doing work when its narration ends.
   `record_margin_seconds` on a `[[section]]` records past the narration so the cut never lands on a
   frame that was never drawn.

## A late landing

The whole point of DeckTalk is that a picture arrives on its word. These codes are how it says a
picture did not.

| Code | Certainty | What a viewer would notice |
|---|---|---|
| [CUE_OFF](/reference/findings/CUE_OFF) | certain | The picture arrives on the wrong word, or visibly after the right one. |
| [PAGE_WORD_LATE](/reference/findings/PAGE_WORD_LATE) | uncertain | A line being shown word by word trails the voice reading it. |
| [CUE_NO_ONSET](/reference/findings/CUE_NO_ONSET) | uncertain | Nothing, yet. The measurement had no onset to work from, so the number is not trustworthy. |
| [CUT_SPEECH](/reference/findings/CUT_SPEECH) | certain | A word is cut in half at a section boundary. |
| [CUT_POP](/reference/findings/CUT_POP) | certain | The picture jumps at a boundary that promised to be seamless. |

**What to do about `CUE_OFF`.**

1. Read the number in the message. It says how far off the change landed and what the limit is.
2. A whole section that is late by the same amount is a machine bias rather than a deck problem. Run
   `decktalk doctor --measure`, which drives a browser to measure this host's presentation bias and
   writes `host.presentation_bias_ms`. Every later run then subtracts it.
3. One cue that is late on its own is usually a phrase that resolves to the wrong word. Run
   `decktalk words` and read what the section really says around it. Set `occurrence` on the row
   when the phrase repeats.
4. `offset` on a `cues.json` row moves one cue by a fixed number of seconds. Use it when the picture
   genuinely belongs slightly before or after its phrase, and not to paper over a phrase that
   matched the wrong place.
5. `verify.cue_offset_max_ms` is the limit, and `verify.av_offset_max_ms` is the audio against video
   limit beside it. Widening a limit hides the measurement rather than fixing the film.

**When the motion itself is the problem.** A cue can only be measured if the motion it starts is over
quickly enough to be read as a step. The ceiling is half a second, and three codes report going past
it.

| Code | What to shorten |
|---|---|
| [PAGE_MOTION_OVERRUN](/reference/findings/PAGE_MOTION_OVERRUN) | `data-in-seconds` on the element, or `motion.scale` for the whole render. |
| [PAGE_STAGGER_OVERRUN](/reference/findings/PAGE_STAGGER_OVERRUN) | `data-stagger`, or the number of children the container staggers. |
| [PAGE_APPEAR_TOO_LONG](/reference/findings/PAGE_APPEAR_TOO_LONG) | The line itself, or swap `data-words` from `appear` to `highlight`. |

`data-in-seconds` runs from 0.12 to 0.48 in steps of 0.04, which is three to twelve frames at the 25
frames per second the recorder captures at. `data-stagger` runs from 0.04 to 0.2 in steps of 0.04,
and a staggered container's total is the step times the children less one, plus the entrance, which
must stay under half a second. `motion.scale` multiplies every declared length, so a value above one
is what pushes a deck that was fine over the ceiling. `decktalk schema page` prints every attribute
with its range.

**`CUT_SPEECH` and `CUT_POP`** are about the seams rather than the reveals. A cut that slices a word
means the section's narration did not have room to finish, and `[narration] tail_min_seconds` is the
silence kept after the last word of a section so that a cut breathes.
`verify.cut_max_dbfs` and `verify.cut_window_seconds` are how quiet, and for how long, a cut has to
be to pass.

`CUT_POP` is measured only where a section declares `seamless = true`, which is the promise that its
picture continues the one before it. The last frame of the previous section and the first frame of
this one are compared, and more than `verify.cut_change_max_percent` of the frame differing means the
promise was not kept. Either make the two pictures actually continue, or drop the promise and let the
boundary be an ordinary cut, which `[transition]` dips through.

## A loudness miss

**What you see.** `MIX_LOUDNESS`, which is uncertain, with the measured numbers in the message.

The mix is mastered to `mix.loudness.target_lufs`, with `mix.loudness.true_peak_max_dbtp` as the peak
ceiling and `mix.loudness.range_max_lu` as the widest loudness range allowed. The defaults are
-16 LUFS, -1.5 dBTP and 11 LU, which are ordinary values for video on the web.

**What to do.**

1. Check whether the loudness pass ran at all. On a build with placeholder narration it does not,
   and the run says so.

   ```text
   The loudness pass is skipped because the narration is a placeholder, so the soundtrack is encoded as it was mixed.
   ```

   Judge loudness on a voiced build.
2. A film that misses the target by a lot usually has a bed fighting the voice. `[mix] music_db`,
   `[mix] music_duck_db` and `[mix] ambience_db` are where that is settled.
3. Change the target only when you are mastering for a platform that asks for a different one. Read
   it whole first with `decktalk config explain mix.loudness.target_lufs`, which prints all five
   layers with the winner marked.

## A page that loads an asset from a network origin

**What you see.** `PAGE_CDN_ASSET`, once per other origin the page reached for, naming that origin.

```text
deck/index.html: PAGE_CDN_ASSET the page loaded an asset from https://fonts.example.com, which the project does not own, so the film depends on a host that may change or stop answering.
```

It is certain and it carries no fix, because only you can decide what the local copy should be.

**What to do.**

1. Download the asset into `deck/` and point the page at the local copy. A font, a stylesheet, a
   script and an image are all the same answer.
2. For equations there is nothing to download. `decktalk init` copies the packaged KaTeX into
   `deck/katex/`, so a page that links those two files loads nothing from a network.
3. Run `decktalk serve` and open the page. That origin serves the deck directory and the files
   `decktalk.toml` declares and nothing else, so anything the page still fetches from elsewhere is
   visible in the browser's network panel.
4. Keep the code if you mean it. `--allow PAGE_CDN_ASSET` carries a run past it, and it is worth
   writing down why, because a film that cannot be rebuilt in a year is what the code is warning
   about.

## A page the contract refuses

These come out whenever `record` or `check` opens the page. Most of them the page reports about
itself, through the probe, and the rest DeckTalk reads off the catalog the page published. All of
them are about markup rather than about timing, so all of them can be cleared before a single credit
is spent. Two of them are marked uncertain in the table, and the rest are certain because the
contract either declares a thing or it does not.

| Code | The change that clears it |
|---|---|
| [PAGE_UNKNOWN_ATTR](/reference/findings/PAGE_UNKNOWN_ATTR) | Spell the attribute the way `decktalk schema page` spells it, or take it off. |
| [PAGE_BAD_VALUE](/reference/findings/PAGE_BAD_VALUE) | Put the value back inside the published range, which the message quotes. |
| [PAGE_MOMENT_UNKNOWN](/reference/findings/PAGE_MOMENT_UNKNOWN) | Declare that cue on the slide the element sits in, or correct the name. |
| [PAGE_MOMENT_ORDER](/reference/findings/PAGE_MOMENT_ORDER) | Give the exit a cue that fires later than the entrance on the same element. |
| [PAGE_NO_OWNER](/reference/findings/PAGE_NO_OWNER) | Name the cue in a moment attribute, or list it in the slide's `data-owns`. |
| [PAGE_SCENE_EMPTY](/reference/findings/PAGE_SCENE_EMPTY) | Add a `<template data-slide>` to the scene, or remove the scene. |
| [PAGE_SLIDE_NO_ID](/reference/findings/PAGE_SLIDE_NO_ID) | Give the template a `data-slide` value. |
| [PAGE_SLIDE_DOUBLED](/reference/findings/PAGE_SLIDE_DOUBLED) | Rename one of the two, and rename its cues in `cues.json` with it. |
| [PAGE_SLIDE_UNUSED](/reference/findings/PAGE_SLIDE_UNUSED) | Have a section play its scene, or delete the slide. Pass `--allow PAGE_SLIDE_UNUSED` while a slide is still being wired up. |
| [PAGE_TEMPLATE_IGNORED](/reference/findings/PAGE_TEMPLATE_IGNORED) | Give the nested template its own `data-slide`, or take it out of the slide. |
| [PAGE_SWAP_AMBIGUOUS](/reference/findings/PAGE_SWAP_AMBIGUOUS) | Give the arriving element and the one it replaces a cue they share. |
| [PAGE_PREVIEW_AMBIGUOUS](/reference/findings/PAGE_PREVIEW_AMBIGUOUS) | Give the two sections different scenes. |
| [PAGE_STAGGER_EMPTY](/reference/findings/PAGE_STAGGER_EMPTY) | Give the container children, or take `data-stagger` off it. |
| [PAGE_WORDS_NOT_FOUND](/reference/findings/PAGE_WORDS_NOT_FOUND) | Make the element's text words the script really speaks in that section. |
| [PAGE_KATEX_MISSING](/reference/findings/PAGE_KATEX_MISSING) | Link `deck/katex/katex.min.css` and `deck/katex/katex.min.js`, which `decktalk init` copied in. |
| [PAGE_KATEX_ERROR](/reference/findings/PAGE_KATEX_ERROR) | Correct the TeX the message quotes. The element's own text is the fallback a reader sees meanwhile. |
| [PAGE_FREEZE_CUE_UNKNOWN](/reference/findings/PAGE_FREEZE_CUE_UNKNOWN) | Name a cue the slide declares in `--after` or `--before`. |
| [PAGE_CLASS_UNDESCRIBED](/reference/findings/PAGE_CLASS_UNDESCRIBED) | Add the matching pair to `data-describe-class`. |
| [PAGE_CLASS_NOT_REDUCED](/reference/findings/PAGE_CLASS_NOT_REDUCED) | Make your stylesheet honour `prefers-reduced-motion` for that class. |
| [PAGE_NO_DESCRIPTION](/reference/findings/PAGE_NO_DESCRIPTION) | Add `data-describe`, or add it empty to say the element is decorative. |
| [PAGE_SWAP_APART](/reference/findings/PAGE_SWAP_APART) | Move the two halves of the swap closer together. This one is uncertain, because a deliberate jump is possible. |

**What to do.** Run `decktalk schema page`, which prints all twenty-four attributes with their
values, their defaults, their ranges and their spans. It is generated from `contract.ts`, the one
home of every attribute, so it cannot disagree with what the runtime does. Then fix the markup.

Two of these are about the transcript rather than the picture. `PAGE_NO_DESCRIPTION` and
`PAGE_CLASS_UNDESCRIBED` mean a reader of the transcript loses a step the viewer saw, and
`data-describe` with an empty value is how you say an element is decorative and meant it.

`PAGE_CLASS_NOT_REDUCED` is about reduced motion. No flag duplicates a settings key, so reduced
motion is `--set motion.reduce=true` on any command that runs against the project, and it asks
Chromium for `prefers-reduced-motion`. The page's own stylesheet is what has to honour that query,
and this code says a class it adds still animates when nobody asked it to.

`PAGE_CLASS_UNDESCRIBED` is the other half of `data-class`. A class change is a change to the
picture, so `data-describe-class` gives each one a phrase, written as moment and phrase pairs
separated by a vertical bar, and the transcript prints it.

## A script that is not ready to be voiced

| Code | Certainty | The change that clears it |
|---|---|---|
| [TAKE_PLACEHOLDER](/reference/findings/TAKE_PLACEHOLDER) | certain | Replace the placeholder on the line the message names with the words you mean. |
| [TAKE_SPOKEN_SYMBOL](/reference/findings/TAKE_SPOKEN_SYMBOL) | uncertain | Write the symbol out as the voice must say it. |

Both come from `decktalk check` reading `script.md`, so both are free to find and both are found
before a credit is spent. This is the cheapest check there is, and running it in a commit hook with
`--no-pages` costs nothing.

Run it before you voice anything, because `narrate` refuses on the same condition. A run that has
already paid cannot take the money back, so a script the voice must not receive is refused rather
than read out. `check` reports every such line at once with its line number, where `narrate` stops
on the first.

`TAKE_SPOKEN_SYMBOL` is uncertain because sometimes you do mean the symbol. Write out what the voice
must say, and [Writing for the ear](/guides/writing-for-the-ear) is the guide for it.

## When the answer is not a code

Some things are not judgements. Use the stream.

- `--events` prints one JSON line per event on stderr as it happens, and every run also appends
  `build/events/<run>.jsonl` whether you passed it or not. The discriminator key is `event` and
  there are eleven names: `run.start`, `run.done`, `stage.start`, `stage.done`, `section.start`,
  `section.done`, `progress`, `finding`, `spend`, `fetch` and `log`.
- `stage.done` and `section.done` carry an `outcome` of `ok`, `skipped` or `failed`, so a run that
  stopped early still says what it did not reach rather than leaving you to infer it from a shorter
  list.
- `output.events_keep_runs` prunes the oldest files, and its default is 20.
- `-v` puts debug lines on stderr and prints the traceback of a bug. `error[INTERNAL]` means a bug in
  DeckTalk, and `-v` is what turns it into something worth reporting.
- `decktalk status` reports where the project stands and what to run next.

## Related

- **Every command, flag and code in one place:** [CLI](/reference/cli)
- **Every settings key with its range and its layer:** [Configuration](/reference/configuration)
- **What a page may declare:** [The page contract](/concepts/page-contract)
- **What `verify` measures and how:** [Verify](/reference/verify)
- **The questions before the first build:** [FAQ](/help/faq)
