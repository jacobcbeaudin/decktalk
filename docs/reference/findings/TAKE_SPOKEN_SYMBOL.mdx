---
title: TAKE_SPOKEN_SYMBOL
description: The script holds a symbol the voice reads as its name rather than as the thing it means.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The script holds a symbol the voice reads as its name rather than as the thing it means.

This finding is uncertain, and DeckTalk measures it from what the run produced.
Pass `--allow TAKE_SPOKEN_SYMBOL` to carry on past it, and `--fail-on` to choose which certainty fails a run.
