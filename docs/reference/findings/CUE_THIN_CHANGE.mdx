---
title: CUE_THIN_CHANGE
description: Less of the picture changed at the cue than a visible reveal must cross.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

Less of the picture changed at the cue than a visible reveal must cross.

This finding is uncertain, and DeckTalk measures it from what the run produced.
Pass `--allow CUE_THIN_CHANGE` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `verify.changed_share_min_percent`, `verify.margin_min_points`, `verify.probe_diff_luma`, `verify.thin_change_factor`, `video.crf`.
