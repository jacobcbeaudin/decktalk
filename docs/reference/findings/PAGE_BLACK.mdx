---
title: PAGE_BLACK
description: A recorded frame is black, so the film shows nothing at that moment.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A recorded frame is black, so the film shows nothing at that moment.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_BLACK` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `verify.after_dip_seconds`, `verify.black_max_luma`.
