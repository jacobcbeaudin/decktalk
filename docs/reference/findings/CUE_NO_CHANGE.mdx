---
title: CUE_NO_CHANGE
description: Nothing in the picture changed at the cue's second, so the reveal never happened.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

Nothing in the picture changed at the cue's second, so the reveal never happened.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow CUE_NO_CHANGE` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `verify.changed_share_min_percent`, `verify.margin_min_points`, `verify.probe_delays_seconds`, `verify.probe_diff_luma`, `video.crf`.
