---
title: PAGE_TRUNCATED
description: A recording stopped before its section's clock ran out, so the film is short of picture.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A recording stopped before its section's clock ran out, so the film is short of picture.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_TRUNCATED` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `record.truncated_slack_seconds`.
