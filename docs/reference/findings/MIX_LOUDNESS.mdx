---
title: MIX_LOUDNESS
description: The mixed film misses the loudness it was mastered to.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The mixed film misses the loudness it was mastered to.

This finding is uncertain, and DeckTalk measures it from what the run produced.
Pass `--allow MIX_LOUDNESS` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `mix.loudness.range_max_lu`, `mix.loudness.target_lufs`, `mix.loudness.true_peak_max_dbtp`.
