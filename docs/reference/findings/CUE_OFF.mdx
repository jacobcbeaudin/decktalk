---
title: CUE_OFF
description: The change lands further from its word than the offset limit allows.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The change lands further from its word than the offset limit allows.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow CUE_OFF` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `host.presentation_bias_ms`, `verify.av_offset_max_ms`, `verify.click_floor_dbfs`, `verify.click_search_seconds`, `verify.cue_offset_max_ms`, `verify.onset_diff_luma`, `verify.onset_rise_points`, `verify.reference_lead_extra_ms`.
