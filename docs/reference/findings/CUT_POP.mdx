---
title: CUT_POP
description: The picture steps at a section cut, so the film pops on the seam.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The picture steps at a section cut, so the film pops on the seam.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow CUT_POP` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `verify.cut_change_max_percent`.
