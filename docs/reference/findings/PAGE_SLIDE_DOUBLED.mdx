---
title: PAGE_SLIDE_DOUBLED
description: Two slide templates carry the same id, so a cue that names it is ambiguous.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

Two slide templates carry the same id, so a cue that names it is ambiguous.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_SLIDE_DOUBLED` to carry on past it, and `--fail-on` to choose which certainty fails a run.
