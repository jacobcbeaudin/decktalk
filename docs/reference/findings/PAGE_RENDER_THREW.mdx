---
title: PAGE_RENDER_THREW
description: A slide's render threw, so the slide is not on screen.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A slide's render threw, so the slide is not on screen.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_RENDER_THREW` to carry on past it, and `--fail-on` to choose which certainty fails a run.
