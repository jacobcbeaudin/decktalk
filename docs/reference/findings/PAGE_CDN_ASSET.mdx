---
title: PAGE_CDN_ASSET
description: The page loads an asset from a network origin, so the film depends on somebody else's server.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The page loads an asset from a network origin, so the film depends on somebody else's server.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_CDN_ASSET` to carry on past it, and `--fail-on` to choose which certainty fails a run.
