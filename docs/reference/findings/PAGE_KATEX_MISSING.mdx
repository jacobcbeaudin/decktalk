---
title: PAGE_KATEX_MISSING
description: The page asks for KaTeX and the library is not loaded, so every formula shows its source.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The page asks for KaTeX and the library is not loaded, so every formula shows its source.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_KATEX_MISSING` to carry on past it, and `--fail-on` to choose which certainty fails a run.
