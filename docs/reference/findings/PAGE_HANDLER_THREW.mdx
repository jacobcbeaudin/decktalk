---
title: PAGE_HANDLER_THREW
description: A moment handler threw, so the change it was to make never happened.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A moment handler threw, so the change it was to make never happened.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_HANDLER_THREW` to carry on past it, and `--fail-on` to choose which certainty fails a run.
