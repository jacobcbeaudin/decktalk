---
title: PAGE_WAIT_UNSETTLED
description: A wait the page declared never settled inside its budget, so the runtime carried on without it.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A wait the page declared never settled inside its budget, so the runtime carried on without it.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_WAIT_UNSETTLED` to carry on past it, and `--fail-on` to choose which certainty fails a run.
