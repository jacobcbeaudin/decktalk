---
title: PAGE_STAGGER_OVERRUN
description: A staggered entrance totals past the measurable ceiling, and the arithmetic that says so is exact.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A staggered entrance totals past the measurable ceiling, and the arithmetic that says so is exact.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_STAGGER_OVERRUN` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `motion.scale`.
