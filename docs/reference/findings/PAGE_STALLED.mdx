---
title: PAGE_STALLED
description: The picture held still for longer than a recorded section ever should.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The picture held still for longer than a recorded section ever should.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_STALLED` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `record.frame_gap_max_ms`.
