---
title: CUT_SPEECH
description: Speech is still sounding at a section cut, so the film slices a word in two.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

Speech is still sounding at a section cut, so the film slices a word in two.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow CUT_SPEECH` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `narration.sound_end_min_run_seconds`, `narration.sound_end_noise_dbfs`, `narration.tail_min_seconds`, `verify.cut_max_dbfs`, `verify.cut_window_seconds`.
