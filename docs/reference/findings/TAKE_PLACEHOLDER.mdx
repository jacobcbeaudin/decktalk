---
title: TAKE_PLACEHOLDER
description: The script still holds an open placeholder, so a voiced run would read it out.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The script still holds an open placeholder, so a voiced run would read it out.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow TAKE_PLACEHOLDER` to carry on past it, and `--fail-on` to choose which certainty fails a run.
