---
title: PAGE_SWAP_AMBIGUOUS
description: Two elements claim the same swap, so the runtime cannot tell which one leaves.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

Two elements claim the same swap, so the runtime cannot tell which one leaves.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_SWAP_AMBIGUOUS` to carry on past it, and `--fail-on` to choose which certainty fails a run.
