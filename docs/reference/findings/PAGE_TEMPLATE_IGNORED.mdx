---
title: PAGE_TEMPLATE_IGNORED
description: A template sits outside every slide, so the runtime never instantiates it.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A template sits outside every slide, so the runtime never instantiates it.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_TEMPLATE_IGNORED` to carry on past it, and `--fail-on` to choose which certainty fails a run.
