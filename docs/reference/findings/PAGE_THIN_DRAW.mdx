---
title: PAGE_THIN_DRAW
description: A frozen slide draws less of the picture than a change must cross to be seen.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A frozen slide draws less of the picture than a change must cross to be seen.

This finding is uncertain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_THIN_DRAW` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `verify.changed_share_min_percent`, `video.crf`.
