---
title: PAGE_MOTION_OVERRUN
description: A motion span runs past the measurable ceiling, so the cue it carries cannot be verified.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A motion span runs past the measurable ceiling, so the cue it carries cannot be verified.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow PAGE_MOTION_OVERRUN` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `motion.scale`.
