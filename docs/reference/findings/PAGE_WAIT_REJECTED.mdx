---
title: PAGE_WAIT_REJECTED
description: A wait the page declared was rejected, so the runtime carried on without what it waited for.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A wait the page declared was rejected, so the runtime carried on without what it waited for.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_WAIT_REJECTED` to carry on past it, and `--fail-on` to choose which certainty fails a run.
