---
title: PAGE_CLASS_NOT_REDUCED
description: A class change declares no reduced form, so a reduced render would animate anyway.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A class change declares no reduced form, so a reduced render would animate anyway.

This finding is certain, and the page reports it from the browser.
Pass `--allow PAGE_CLASS_NOT_REDUCED` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `motion.reduce`.
