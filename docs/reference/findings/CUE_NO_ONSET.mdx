---
title: CUE_NO_ONSET
description: The cue resolved with no measured onset, so its second is the section's start and not its word's.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The cue resolved with no measured onset, so its second is the section's start and not its word's.

This finding is uncertain, and DeckTalk measures it from what the run produced.
Pass `--allow CUE_NO_ONSET` to carry on past it, and `--fail-on` to choose which certainty fails a run.

The settings that move this verdict are `verify.onset_diff_luma`, `verify.onset_rise_points`.
