---
title: CUE_MISSING
description: The page declares a moment that cues.json does not list, so nothing gives it a second.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The page declares a moment that cues.json does not list, so nothing gives it a second.

This finding is certain, and DeckTalk measures it from what the run produced.
Pass `--allow CUE_MISSING` to carry on past it, and `--fail-on` to choose which certainty fails a run.
