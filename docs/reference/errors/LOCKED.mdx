---
title: LOCKED
description: Another writer holds this project's build directory.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

Another writer holds this project's build directory.

A command that ends this way exits 3. The JSON object carries `error.code` of
`LOCKED`, one sentence in `error.message`, and the whole command that would clear it in
`error.hint`.
