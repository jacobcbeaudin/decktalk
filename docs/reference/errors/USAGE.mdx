---
title: USAGE
description: The command line was refused, and a retry as written fails again.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

The command line was refused, and a retry as written fails again.

A command that ends this way exits 2. The JSON object carries `error.code` of
`USAGE`, one sentence in `error.message`, and the whole command that would clear it in
`error.hint`.
