---
title: INTERNAL
description: A bug in DeckTalk, with a traceback under -v.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A bug in DeckTalk, with a traceback under -v.

A command that ends this way exits 3. The JSON object carries `error.code` of
`INTERNAL`, one sentence in `error.message`, and the whole command that would clear it in
`error.hint`.
