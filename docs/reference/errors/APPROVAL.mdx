---
title: APPROVAL
description: A spend needed an approval that no flag and no terminal gave.
---

{/* Generated by scripts/build_code_pages.py from the enums in src/decktalk. Do not edit by hand. */}

A spend needed an approval that no flag and no terminal gave.

A command that ends this way exits 2. The JSON object carries `error.code` of
`APPROVAL`, one sentence in `error.message`, and the whole command that would clear it in
`error.hint`.
