{
  "name": "decktalk-media",
  "private": true,
  "description": "The Worker that serves the DeckTalk demo film at media.decktalk.app through signed links.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.132.0"
  }
}
