{
  "name": "decktalk-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "The TypeScript sources of the deck runtime and the recorder's probe, and the pinned tools that build them.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/decktalk/runtime/src/*.test.ts' 'tests/scripts/*.test.mjs'",
    "typecheck": "tsc --noEmit -p src/decktalk/runtime/tsconfig.json",
    "check": "biome ci ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/node": "26.6.2",
    "esbuild": "0.28.2",
    "release-please": "17.6.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22.18"
  }
}
