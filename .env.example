# Copy to .env in your project directory (git-ignored). Never commit the real keys.
# ElevenLabs: `cuecut narrate` (TTS with word timestamps) and `cuecut soundscape` (sfx + music).
ELEVENLABS_API_KEY=<your-elevenlabs-key>
ELEVENLABS_VOICE_ID=<voice-id>
# Optional, only for `cuecut broll` (text-to-video). Either one; Gemini is preferred.
GEMINI_API_KEY=<optional-gemini-key>
FAL_KEY=<optional-fal-key>
