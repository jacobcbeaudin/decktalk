<!-- One person maintains DeckTalk. Open an issue before a large change, so the design lands first. -->

## What this changes

<!-- One sentence on the behaviour a user or an agent sees, not on the diff. -->

## Why

<!-- The issue this closes, or the problem it solves. -->

## How it was checked

- [ ] `uv run scripts/check.py` passes.
- [ ] A test covers the behaviour, and it fails without the change.
- [ ] Every generated page is regenerated by its script, never edited by hand.
- [ ] The title follows Conventional Commits, because a squash merge uses it as the commit message.
